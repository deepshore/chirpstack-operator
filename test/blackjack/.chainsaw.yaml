apiVersion: chainsaw.kyverno.io/v1alpha2
kind: Configuration
metadata:
  name: config
spec:
  timeouts:
    apply: 45s
    assert: 20s
    cleanup: 90s
    delete: 25s
    error: 10s
    exec: 45s
  cleanup:
    skipDelete: false
  execution:
    failFast: true
    parallel: 8
